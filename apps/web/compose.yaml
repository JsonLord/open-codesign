services:
  codesign-web:
    build:
      context: ../..
      dockerfile: apps/web/Dockerfile
    ports:
      - "7860:7860"
    environment:
      CODESIGN_PROJECTS_DIR: /app
      PORT: 7860
    volumes:
      - codesign-web-data:/app
    restart: unless-stopped

volumes:
  codesign-web-data:
